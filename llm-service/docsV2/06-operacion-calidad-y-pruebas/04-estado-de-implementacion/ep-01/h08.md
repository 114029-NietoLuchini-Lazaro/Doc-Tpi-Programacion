# LLM-S01-H08 — Contrato OpenAPI y mock del golden set publicados

- **Estado:** ✅ Cumplido
- **Ficha:** [`docs/historias/ep-01/h05.md`](../../../07-planificacion-y-trabajo-equipo/09-epicas-historias-tareas-sprints/historias/ep-01/h05.md) (LLM-EP01-H05, ex-H08)
- **Evidencia:** [`CORRECCIONES-SUGERIDAS.md` ítems 11–13](../../../CORRECCIONES-SUGERIDAS.md), [`checklist-cierre-s1.md` §2.4–§2.5](../../../docs/entregas/checklist-cierre-s1.md) y [`MOCK.md`](../../../docs/contracts/MOCK.md)

## Qué hay en el código

- `docs/contracts/` tiene los contratos publicados, incluyendo el contrato vigente `llm-service-v2-golden-set.openapi.yaml` y la especificación asincrónica `llm-service-v1.asyncapi.yaml`.
- **T1 Resuelta:** `llm-service-v1.openapi.yaml` y su adenda fueron formalmente marcados como históricos/deprecados y reemplazados por v2 (`checklist-cierre-s1.md` §2.5). El contrato `llm-service-v2-golden-set.openapi.yaml` fue sincronizado con los 3 endpoints reales implementados (`POST .../next-version`, `GET /courses`, `GET .../model-deployments`) y validado con `@apidevtools/swagger-cli`.
- **T2 Resuelta:** La adenda y campos fueron aprobados por `admin-service` (registrado el 2026-09-13 en `PREGUNTAS-ABIERTAS.md`).
- **T3 Resuelta:** Existe el documento [`docs/contracts/MOCK.md`](../../../docs/contracts/MOCK.md) con el comando de una línea de Prism (`npx @stoplight/prism-cli mock ... --port 4010`) y Docker, además del modo `compose.workbench.yaml` con `WorkbenchDemoCatalog`.
- **T4 Resuelta:** `LegacyGoldenSetRouteAbsentTest` garantiza que las rutas viejas o no acordadas son rechazadas activamente con 404.

## Veredicto por CA

| CA | Estado |
|---|---|
| CA1 — OpenAPI válido, describe solo lo implementado | ✅ Cumplido (`llm-service-v2-golden-set.openapi.yaml` sincronizado y válido; v1 marcado histórico) |
| CA2 — mock levantable con un comando | ✅ Cumplido (documentado en `docs/contracts/MOCK.md` y perfil `workbench`) |
| CA3 — adenda S1 revisada con `admin-service` | ✅ Cumplido (aprobada según `PREGUNTAS-ABIERTAS.md` 2026-09-13) |
| CA4 — PR con operación no implementada se rechaza | ✅ Cumplido (`LegacyGoldenSetRouteAbsentTest` en suite) |
| CA5 — campo no acordado bloquea publicación | ✅ Cumplido (acordado y congelado) |
