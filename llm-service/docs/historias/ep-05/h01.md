# G03 — Recibir una respuesta socrática del tutor, filtrada por los dos guardarraíles

- **ID interno:** LLM-EP05-H01 (histórico: LLM-S05-H01 · Sprint: S1/S5)
- **Épica:** EP-05 · Tutor seguro y guardarraíles
- **Pareja:** P3 + P2
- **Dependencias:** `LLM-EP02-H01` (ex `LLM-S01-H10`, puerto de invocación, ✅), `LLM-S06-H01`/`s05-h02` opcional
  (histórico de conversación)
- **Estimación (plan):** sin paquete propio en ninguna receta existente — EP-05 no tenía ficha de
  HU hasta esta sesión. Código ya construido el 2026-09-12; referencia retroactiva ~35–45 h
  (comparable a `LLM-S01-H10`, 32 h, más los dos guardarraíles).
- **Tipo:** **Historia de usuario** (HU de valor)
- **Requisito:** RF-IA-01/02/04/19/20; [épica EP-05](../../epicas/ep-05.md)
- **Referente de producto:** *(a nombrar en Sprint 0)*

> **Historia escrita a posteriori.** El código (`TutorInteractionController`,
> `TutorInteractionService`, `domain/ai/InputGuard`, `domain/ai/OutputAntiLeakGuard`) se portó
> de `codigo-ejemplo/ms-evaluacion-llm` (`TutorServiceImpl`/`InputGuard`/`OutputAntiLeakGuard`,
> carpeta ya eliminada) el 2026-09-12. Esta ficha documenta lo ya construido — no es
> planificación a futuro. Evidencia completa:
> [`docs/estado-implementacion/ep-05/interactions.md`](../../estado-implementacion/ep-05/interactions.md).

---

## Descripción (Como / Quiero / Para)

- **Como:** alumno que está resolviendo un desafío práctico
- **Quiero:** preguntarle al tutor y recibir una respuesta socrática (que me guíe, no que me dé
  la solución), que además esté protegida contra intentos de manipular al modelo o de sacarle la
  solución esperada
- **Para:** avanzar en el desafío con ayuda real sin que el tutor se convierta en un atajo para
  copiar la respuesta

---

## Notas / Observaciones

- **Reglas de trabajo:** cada pedido lleva una `Idempotency-Key` — repetirla devuelve la misma
  respuesta sin volver a invocar al modelo. Antes de invocar, un guardarraíl de entrada revisa si
  el mensaje es un intento de manipular al tutor (jailbreak); si lo es, el tutor corta ahí, con
  un mensaje fijo, y **nunca** llega a invocar al modelo.
- **Cómo se controla la salida:** cuando el riesgo de la interacción es medio o alto (no en
  riesgo bajo), la respuesta del modelo se revisa antes de mostrarla — si contiene una fuga hacia
  la solución esperada, se reemplaza por una redirección socrática, sin que el alumno vea nunca
  el texto original.
- **Qué tiene que incluir sí o sí:** el contenido del mensaje, el curso/cohorte y el alumno, y
  opcionalmente a qué conversación pertenece — si no se indica conversación, igual queda una
  registrada por detrás, de un solo turno.
- **Con historial:** si el pedido indica una conversación existente, el tutor carga los últimos
  turnos de esa conversación y los usa de contexto — la respuesta cambia según lo que se habló
  antes, no trata cada mensaje como si fuera el primero.
- **Auditoría:** cada interacción queda registrada con su nivel de riesgo, su estado final, y si
  se disparó algún guardarraíl — para poder revisar después qué pasó, sin adivinar.
- **Tiempos / volumen:** el envío de la respuesta en un solo bloque (no en vivo, palabra por
  palabra) es una decisión explícita de esta versión — mostrar la respuesta "en vivo" es una
  historia aparte, todavía no construida.
- **Seguridad:** el pedido tiene que venir autenticado como un servicio de confianza que actúa en
  nombre del alumno — no cualquiera puede invocar al tutor a nombre de otro.
- **Accesibilidad:** no evaluada en esta revisión.

---

## Criterios de Aceptación (CA)

- [x] **CA1 — hecho:** un pedido con `Idempotency-Key` nueva invoca al tutor y devuelve una
  respuesta socrática dentro del schema esperado.
- [x] **CA2 — hecho (caso que debe fallar convertido en éxito):** repetir la misma
  `Idempotency-Key` devuelve la misma respuesta sin invocar al modelo de nuevo.
- [x] **CA3 — hecho:** un mensaje detectado como intento de manipular al tutor nunca llega a
  invocar al modelo — responde con un mensaje fijo.
- [x] **CA4 — hecho:** una respuesta del modelo con riesgo medio o alto que contiene una fuga
  hacia la solución esperada se reemplaza por una redirección socrática antes de mostrarse.
- [x] **CA5 — hecho:** si el pedido indica una conversación existente, la respuesta se arma
  considerando los últimos turnos de esa conversación.
- [ ] **CA6 (caso que debe fallar, pendiente):** un pedido sin autenticación de servicio de
  confianza válida se rechaza — cubierto por `TutorGatewayAuthorization`, sin test dedicado
  todavía a nivel de esta ficha.

---

## BDD

**Qué se prueba:** que el tutor responda de forma socrática, con los dos guardarraíles activos y
sin repetir una invocación innecesaria.

### Escenario 1 — Pregunta válida (camino feliz, cubierto)

- **Dado:** un alumno con un pedido nuevo, sin señales de manipulación
- **Cuando:** envía su pregunta al tutor
- **Entonces:** recibe una respuesta socrática, y la interacción queda auditada

### Escenario 2 — Intento de manipular al tutor (cubierto)

- **Dado:** un mensaje que intenta hacer que el tutor ignore sus reglas
- **Cuando:** se envía al tutor
- **Entonces:** el tutor responde con el mensaje fijo de corte, sin invocar al modelo

### Escenario 3 — La respuesta del modelo se acerca demasiado a la solución (cubierto)

- **Dado:** una interacción de riesgo medio o alto cuya respuesta generada contiene la solución
  esperada
- **Cuando:** el guardarraíl de salida la revisa
- **Entonces:** el alumno recibe una redirección socrática, nunca el texto con la fuga

---

## Prototipo

- **Capturas / bocetos:** no auditadas en esta revisión — el tutor se consume hoy vía contrato,
  sin pantalla propia relevada acá.
- **Maqueta / documentación:** [`docs/contracts/llm-service-v1.openapi.yaml`](../../contracts/llm-service-v1.openapi.yaml)
  (`TutorInteractionRequest`/`Response`), [adenda SSE](../../contracts/llm-service-v1-tutor-sse-adenda.md)
  (streaming, todavía fuera de este camino).

---

## Estimación / Prioridad

**Formato rápido:**

- **Puntos (Fibonacci):** *(a fijar en Refinamiento)*
- **Prioridad (MoSCoW / numérica):** **Must** — es el ítem #6 del alcance obligatorio (salvaguarda
  anti-fuga), ya construido

**En palabras:**

- **Puntos de esfuerzo:** se asignan en Refinamiento contra la historia canónica, a pesar de
  estar ya construida — el equipo igual necesita el número para la trazabilidad de Taiga.
- **Prioridad:** imprescindible, y ya cumplida.

---

## Dependencias / Impactos

- **Partes involucradas:** `ModelInvocationPort` (EP-02), `InputGuard`, `OutputAntiLeakGuard`,
  `IdempotencyRepository` (reutilizada, no nueva), `TutorGatewayAuthorization`.
- **Otros equipos / aprobaciones:** ninguna nueva — el contrato ya está acordado.
- **Impacto en los datos:** tabla `audit_events` (reutilizada) por interacción.
- **Riesgos:** hoy corre siempre contra `FakeModelAdapter` — ningún caso de esta historia se
  probó contra un proveedor real todavía (ver `LLM-S03-H11`).
